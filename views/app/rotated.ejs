<% layout('layout/boilerplate.ejs') %>

<section class="card warning">

  <h1>Client Secret Rotated 🔑</h1>

  <p class="danger">
    Your previous client secret has been <strong>invalidated</strong>.
    Copy the new secret now. You won’t be able to see it again.
  </p>

  <div class="credential-box">
    <label>Client ID</label>
    <code><%= app.clientId %></code>
  </div>

  <div class="credential-box highlight">
    <label>New Client Secret</label>
    <code id="secret"><%= clientSecret %></code>
    <button onclick="copySecret()">Copy</button>
  </div>

  <a href="/app/<%= app._id %>" class=" btn btn-primary">
    <span class="text-white">I’ve updated my environment</span>
  </a>

</section>

<script>
  function copySecret() {
    const text = document.getElementById('secret').innerText;
    navigator.clipboard.writeText(text);
    alert('Client secret copied!');
  }
</script>
